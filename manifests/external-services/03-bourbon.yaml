apiVersion: v1
kind: Service
metadata:
  name: bourbon
  namespace: homelab
  annotations:
    tailscale.com/hostname: bourbon
    tailscale.com/tags: "tag:domain-lab-shortrib-net"
    tailscale.com/expose: "true"
spec:
  ports:
  - name: https
    port: 443
    protocol: TCP
    targetPort: 443
---
apiVersion: discovery.k8s.io/v1
kind: EndpointSlice
metadata:
  name: bourbon-slice
  namespace: homelab
  labels:
    kubernetes.io/service-name: bourbon  # Must match Service name
addressType: IPv4
ports:
  - name: https
    protocol: TCP
    port: 443
endpoints:
  - addresses:
      - 10.18.1.220
    conditions:
      ready: true
